[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "feedforge"
version = "0.1.38"
description = "Desktop toolkit for FeedPak files for the FeedBack game."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "construct>=2.10,<3",
    "cryptography>=42",
    "jsonschema>=4",
    "Pillow>=10",
    "PyYAML>=6",
    "soundfile>=0.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pyinstaller>=6",
]
stems = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "python-multipart>=0.0.9",
    "demucs>=4.0",
]

[project.scripts]
psarc2feedpak = "feedback_converter.cli:main"
feedforge-demucs-server = "feedback_converter.demucs_server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
feedback_converter = ["tools/*", "data/*.json", "data/feedpak_schemas/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
